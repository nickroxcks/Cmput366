To test the code, simply move to the directory of the main file and run:

python main.py

This will result in 2 plots being generated for the top 95 problems. The solutions to each problem
are printed to console to allow you to verify the functionaility of the algorithm. 